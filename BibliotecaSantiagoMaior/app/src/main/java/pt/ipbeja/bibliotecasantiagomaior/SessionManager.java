package pt.ipbeja.bibliotecasantiagomaior;

import android.content.Context;

public class SessionManager {

    /*public static boolean isSessionActive(Context context) {
        // TODO utilizar SharedPrefs para verificar se existe um utilizador logado
        // devolvemos True caso exista, False caso não exista
    }

    public static void registerSession(Context context, User user) {
        // TODO guardar nas SharedPreferences o que entermos acerca do user
    }

    public static User getActiveSession(Context context) {
        // TODO devolve um user caso esteja guardado nas SharedPrefernces
        // null caso não exista user
    }*/
}
